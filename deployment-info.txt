Application successfully built and deployed to Firebase.
Hosting URL: https://blue-journal.web.app
Deployment date: Fri Jul 19 2024

Changes in this deployment:
1. Fixed install prompt behavior:
   - Modified app to show install prompt only once per session
   - Added persistence of user's choice in localStorage
   - Added install button to settings page for users who initially reject the prompt

2. Fixed dark mode text readability:
   - Improved contrast for warning text in settings page
   - Added RGB color variables for better alpha transparency support
   - Made warning text bold for better readability

3. Fixed dialog styling in dark mode:
   - Updated dialog styles to match mat-cards in dark mode
   - Ensured consistent styling across all dialogs in the application

Previous changes:
1. Fixed build.sh script:
   - Made Firebase deployment optional for local development
   - Added checks for CI environment
   - Improved error handling and user feedback

2. Fixed serviceWorker configuration:
   - Updated angular.json to use correct serviceWorker configuration
   - Changed from boolean value to string pointing to ngsw-config.json

3. Improved dark mode styling:
   - Enhanced button visibility in dark mode
   - Fixed styling for "Add Image" and other buttons
   - Updated settings page elements to use CSS variables
   - Added proper contrast for text and UI elements in dark mode

3. Previous changes:
   - Fixed dark mode text readability issues
   - Set up PWA icon using blueprinceicon.png
   - Added git commit hash display at the bottom of every page
   - Updated build script to generate PWA icons and insert git commit hash
   - Enabled service worker for better offline support
